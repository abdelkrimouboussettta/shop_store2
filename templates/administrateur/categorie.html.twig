{% extends 'baseadmin.html.twig' %}

{% block title %}Liste des categorie{% endblock %}

{% block body %}
     

<a href="{{path('admin.form.categorie')}}" class="btn btn-primary"> nouvel categorie </a>                                              

        {% for categorie in categories %}

            <div>

                
                
                    <h2>{{categorie.titre}}</h2>
        <h2>{{categorie.resume}}</h2>
                        
<a href="{{path('admin.categorie.modif', {'id':categorie.id})}}" class="btn btn-primary"> modifier </a>                                              
<a href="{{path('admin.categorie.sup', {'id':categorie.id})}}" class="btn btn-primary"> Supprimer </a>                                              
                
            </div>

        {% endfor %}
                        
        
        </div>

<div class="navigation">
    {{ knp_pagination_render(categories) }}
</div>
{% endblock %}
