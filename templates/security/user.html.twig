{% extends 'baseadmin.html.twig' %}

{% block title %}Liste des user{% endblock %}

{% block body %}
     

<a href="{{path('user.form')}}" class="btn btn-primary"> nouveau utilisateur </a>                                              
<table>
<tr>
<th>id</th>
<th>nom</th>
<th>mail</th>
</tr>

        {% for user in users %}
<tr>
<td>{{user.id}}</td>
<td>{{user.nom}}</td>
<td>{{user.mail}}</td>
    <td><a href="{{path('user.modif', {'id':user.id})}}" class="btn btn-info"> modifier </a></td>                                              
<td><a href="{{path('user.sup', {'id':user.id})}}" class="btn btn-danger"> Supprimer </a></td>                                              
                </tr>
            
        {% endfor %}
            </table>            
        
        </div>

<div class="navigation">
    
</div>
{% endblock %}
